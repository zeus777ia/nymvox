# Minimal Supabase config so `supabase functions deploy` / local emulators work.
# Full project config is generated by `supabase init` if you use the CLI locally.

project_id = "nymvox"

[functions.ai-generate]
verify_jwt = false

[functions.social-oauth]
verify_jwt = false

[functions.stripe-webhook]
verify_jwt = false
